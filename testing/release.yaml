version: '1.0'

steps:
  main_clone:
    type: git-clone
    repo: open-integration/testing
    git: cf_github
    revision: ${{CF_REVISION}}


  build:
    type: build
    image_name: openintegration/testing
    tag: latest
    registry: dockerhub